<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout  xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/story_container"
    android:layout_width="@dimen/story_item_width"
    android:layout_height="@dimen/story_item_height"
    android:layoutDirection="ltr"
    android:layout_marginEnd="5dp"
    >

    <ImageView
        android:id="@+id/story_image"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:scaleType="centerCrop"
        android:transitionName="imageStory"
        android:src="@drawable/placeholder"
        />

    <RelativeLayout
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerHorizontal="true"
        android:layout_alignParentBottom="true"
        android:layout_marginBottom="10dp"
        >

        <de.hdodenhof.circleimageview.CircleImageView
            android:id="@+id/user_image"
            android:layout_width="@dimen/story_avatar"
            android:layout_height="@dimen/story_avatar"
            android:layout_centerInParent="true"
            android:src="@drawable/logo_btn_home"
            android:transitionName="imageMain"
            />

        <View
            android:id="@+id/user_border"
            android:layout_width="@dimen/story_avatar_border"
            android:layout_height="@dimen/story_avatar_border"
            android:background="@drawable/story_border"
            />

    </RelativeLayout>
</RelativeLayout>